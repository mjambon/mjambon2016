open Printf

let super_formula x =
  let ( + ) = ( +. )
  and ( - ) = ( -. )
  and ( / ) = ( /. )
  and ( * ) = ( *. )
  and ( mod ) = mod_float in
  1.23 * 10. - x

let _ = printf "%g\n" (super_formula 3.)
